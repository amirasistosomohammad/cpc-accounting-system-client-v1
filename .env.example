# API base URL – one env var for both local and production
# Local: use this in .env
VITE_LARAVEL_API=http://localhost:8000/api

# Production: set this in your build environment (e.g. DigitalOcean env) to your API URL, then rebuild
# VITE_LARAVEL_API=https://cpc-server-4ckzd.ondigitalocean.app/api
