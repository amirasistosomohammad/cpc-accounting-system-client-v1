# API base URL (use either variable; both are read by the app).
# Local development:
VITE_LARAVEL_API=http://localhost:8000/api
# Or: VITE_API_URL=http://localhost:8000/api

# Production: set ONE of these in your build environment (e.g. DigitalOcean App env vars)
# and trigger a new build so the value is embedded. Example:
# VITE_LARAVEL_API=https://cpc-server-4ckzd.ondigitalocean.app/api
# VITE_API_URL=https://cpc-server-4ckzd.ondigitalocean.app/api
